{
  "name": "threejs-terrain-editor",
  "version": "0.1.0",
  "description": "Thee JS Terrain Editor",
  "main": "main.js",
  "scripts": {
    "start": "npm run bundle && electron .",
    "watch": "npm run bundle && watch -p \"assets/scripts/**/*.js\" -c \"npm run bundle\"",
    "bundle": "node bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nodesecure/eslint-config": "^1.9.0",
    "esbuild": "^0.19.5",
    "eslint": "^8.53.0",
    "watch-cli": "^0.2.3"
  },
  "dependencies": {
    "electron": "^28.0.0",
    "three": "^0.158.0"
  }
}
